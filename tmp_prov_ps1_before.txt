[CmdletBinding()]param(    [Parameter(ValueFromRemainingArguments = $true)]    [string[]]$ScriptArgs)$ErrorActionPreference = "Stop"function Import-EnvFile {    param([Parameter(Mandatory = $true)][string]$Path)    if (-not (Test-Path -Path $Path -PathType Leaf)) {        throw "Environment file not found: $Path"    }    foreach ($line in Get-Content -Path $Path) {        $trimmed = $line.Trim()        if ([string]::IsNullOrWhiteSpace($trimmed) -or $trimmed.StartsWith("#")) {            continue        }        if ($trimmed -match "^(?:export\s+)?([A-Za-z_][A-Za-z0-9_]*)=(.*)$") {            $name = $Matches[1]            $value = $Matches[2].Trim()            if (                ($value.StartsWith("'") -and $value.EndsWith("'")) -or                ($value.StartsWith('"') -and $value.EndsWith('"'))            ) {                $value = $value.Substring(1, $value.Length - 2)            }            [Environment]::SetEnvironmentVariable($name, $value)        }    }}function Require-Env {    param([Parameter(Mandatory = $true)][string]$Name, [string]$Message = "")    $value = [Environment]::GetEnvironmentVariable($Name)    if ([string]::IsNullOrWhiteSpace($value)) {        if ($Message) {            throw $Message        }        throw "Set $Name"    }}function Invoke-Checked {    param(        [Parameter(Mandatory = $true)][string]$FilePath,        [Parameter(ValueFromRemainingArguments = $true)][string[]]$Arguments    )    & $FilePath @Arguments    if ($LASTEXITCODE -ne 0) {        throw "Command failed ($LASTEXITCODE): $FilePath $($Arguments -join ' ')"    }}function Test-CommandSuccess {    param(        [Parameter(Mandatory = $true)][string]$FilePath,        [Parameter(ValueFromRemainingArguments = $true)][string[]]$Arguments    )    & $FilePath @Arguments *> $null    return $LASTEXITCODE -eq 0}$envPath = Join-Path $PSScriptRoot "cloud-run.env"Import-EnvFile -Path $envPathRequire-Env GCP_PROJECT_ID "Set GCP_PROJECT_ID"Require-Env GCP_REGION "Set GCP_REGION, for example us-central1"Require-Env GCP_AR_REPOSITORY "Set GCP_AR_REPOSITORY"Require-Env RUN_SERVICE_ACCOUNT "Set RUN_SERVICE_ACCOUNT"Require-Env VPC_NETWORK "Set VPC_NETWORK, for example default"Require-Env PRIVATE_RANGE_NAME "Set PRIVATE_RANGE_NAME"Require-Env PRIVATE_RANGE_PREFIX "Set PRIVATE_RANGE_PREFIX, for example 16"Require-Env CLOUD_SQL_INSTANCE "Set CLOUD_SQL_INSTANCE"Require-Env CLOUD_SQL_DB "Set CLOUD_SQL_DB"Require-Env CLOUD_SQL_USER "Set CLOUD_SQL_USER"Require-Env CLOUD_SQL_PASSWORD "Set CLOUD_SQL_PASSWORD"$cloudSqlVersion = if ($env:CLOUD_SQL_VERSION) { $env:CLOUD_SQL_VERSION } else { "POSTGRES_18" }$cloudSqlEdition = if ($env:CLOUD_SQL_EDITION) { $env:CLOUD_SQL_EDITION } else { "enterprise" }$runServiceAccountEmail = "$($env:RUN_SERVICE_ACCOUNT)@$($env:GCP_PROJECT_ID).iam.gserviceaccount.com"Invoke-Checked gcloud config set project $env:GCP_PROJECT_IDInvoke-Checked gcloud services enable `    run.googleapis.com `    cloudfunctions.googleapis.com `    eventarc.googleapis.com `    pubsub.googleapis.com `    artifactregistry.googleapis.com `    cloudbuild.googleapis.com `    sqladmin.googleapis.com `    storage.googleapis.com `    servicenetworking.googleapis.com `    compute.googleapis.comif (-not (Test-CommandSuccess gcloud artifacts repositories describe $env:GCP_AR_REPOSITORY "--location=$($env:GCP_REGION)")) {    Invoke-Checked gcloud artifacts repositories create $env:GCP_AR_REPOSITORY `        "--location=$($env:GCP_REGION)" `        --repository-format=docker `        --description "Container images for CAPanel services"}if (-not (Test-CommandSuccess gcloud iam service-accounts describe $runServiceAccountEmail)) {    Invoke-Checked gcloud iam service-accounts create $env:RUN_SERVICE_ACCOUNT --display-name "CAPanel Cloud Run runtime"}Invoke-Checked gcloud projects add-iam-policy-binding $env:GCP_PROJECT_ID `    "--member=serviceAccount:$runServiceAccountEmail" `    --role roles/cloudsql.clientInvoke-Checked gcloud projects add-iam-policy-binding $env:GCP_PROJECT_ID `    "--member=serviceAccount:$runServiceAccountEmail" `    --role roles/artifactregistry.readerInvoke-Checked gcloud projects add-iam-policy-binding $env:GCP_PROJECT_ID `    "--member=serviceAccount:$runServiceAccountEmail" `    --role roles/run.developerInvoke-Checked gcloud projects add-iam-policy-binding $env:GCP_PROJECT_ID `    "--member=serviceAccount:$runServiceAccountEmail" `    --role roles/storage.objectViewerif (-not (Test-CommandSuccess gcloud compute addresses describe $env:PRIVATE_RANGE_NAME --global)) {    Invoke-Checked gcloud compute addresses create $env:PRIVATE_RANGE_NAME `        --global `        --purpose=VPC_PEERING `        "--prefix-length=$($env:PRIVATE_RANGE_PREFIX)" `        "--network=$($env:VPC_NETWORK)"}$peerServices = @(    & gcloud services vpc-peerings list "--network=$($env:VPC_NETWORK)" --format "value(service)")if ($LASTEXITCODE -ne 0) {    throw "Failed to list VPC peerings."}if (-not ($peerServices -contains "servicenetworking.googleapis.com")) {    Invoke-Checked gcloud services vpc-peerings connect `        --service=servicenetworking.googleapis.com `        "--network=$($env:VPC_NETWORK)" `        "--ranges=$($env:PRIVATE_RANGE_NAME)"}if (-not (Test-CommandSuccess gcloud sql instances describe $env:CLOUD_SQL_INSTANCE)) {    Invoke-Checked gcloud sql instances create $env:CLOUD_SQL_INSTANCE `        "--database-version=$cloudSqlVersion" `        "--edition=$cloudSqlEdition" `        --cpu=1 `        --memory=3840MiB `        "--region=$($env:GCP_REGION)" `        --availability-type=zonal `        --storage-size=20GB `        --storage-type=SSD `        "--network=$($env:VPC_NETWORK)" `        --no-assign-ip}if (-not (Test-CommandSuccess gcloud sql databases describe $env:CLOUD_SQL_DB "--instance=$($env:CLOUD_SQL_INSTANCE)")) {    Invoke-Checked gcloud sql databases create $env:CLOUD_SQL_DB "--instance=$($env:CLOUD_SQL_INSTANCE)"}$sqlUsers = @(    & gcloud sql users list "--instance=$($env:CLOUD_SQL_INSTANCE)" --format "value(name)")if ($LASTEXITCODE -ne 0) {    throw "Failed to list Cloud SQL users."}if (-not ($sqlUsers -contains $env:CLOUD_SQL_USER)) {    Invoke-Checked gcloud sql users create $env:CLOUD_SQL_USER `        "--instance=$($env:CLOUD_SQL_INSTANCE)" `        "--password=$($env:CLOUD_SQL_PASSWORD)"}Write-Host "Provisioning complete."Write-Host "Artifact Registry: $($env:GCP_REGION)-docker.pkg.dev/$($env:GCP_PROJECT_ID)/$($env:GCP_AR_REPOSITORY)"Write-Host "Cloud SQL connection: $($env:GCP_PROJECT_ID):$($env:GCP_REGION):$($env:CLOUD_SQL_INSTANCE)"